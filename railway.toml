[build]
builder = "nixpacks"
buildCommand = "bun install && bun run --cwd frontend build && cd backend && bunx prisma generate"

[deploy]
startCommand = "cd backend && npx prisma migrate deploy && bun src/index.ts"
