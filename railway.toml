[build]
builder = "nixpacks"
buildCommand = "sh -c 'bun install && bun run --cwd frontend build && cd backend && bunx prisma generate'"

[deploy]
startCommand = "sh -c 'cd backend && bunx prisma migrate deploy && bun src/index.ts'"
