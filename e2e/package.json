{
  "name": "helpdesk-e2e",
  "version": "1.0.0",
  "description": "Playwright end-to-end tests for the AI Helpdesk",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report",
    "test:list": "playwright test --list"
  },
  "dependencies": {
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "@types/node": "^20.0.0",
    "@types/pg": "^8.11.0"
  }
}
